const express = require('express');\nconst router = express.Router();\nconst Credit = require('../models/credit');\n\nrouter.post('/', async (req, res) => {\n  try {\n    const { amount, installments } = req.body;\n    const credit = new Credit({ amount, installments });\n    const monthlyPayment = credit.calculateMonthlyPayment();\n    const totalInterest = credit.calculateTotalInterest();\n    res.json({ monthlyPayment, totalInterest });\n  } catch (error) {\n    console.error(error);\n    res.status(500).json({ message: 'Error al calcular la simulación de crédito' });\n  }\n});\n\nmodule.exports = router;